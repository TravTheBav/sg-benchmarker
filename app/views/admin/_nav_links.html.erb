<div class="container text-center">
    <p>
        <%= link_to 'Admin', admin_path %><%= " > #{params[:action].capitalize}" unless params[:action].eql?("index") %>
    </p>
    <div class="btn-group flex-wrap gap-3 mb-5">
        <%= render "admin/link", resource: User %>
        <%= render "admin/link", resource: BuildOrder %>
        <%= render "admin/link", resource: AttackBenchmark %>
    </div>
</div>